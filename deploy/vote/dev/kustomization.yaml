apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base

namespace: instavote

images:
  - name: schoolofdevops/vote-xxxxx
    newTag: v4

replicas:
  - name: vote
    count: 2

configMapGenerator:
  - name: vote
    literals:
      - OPTION_A=FluxCD
      - OPTION_B=ArgoCD
